<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record model="ir.ui.view" id="view_stock_quant_edit_tree_volume_inherit">
        <field name="name">stock.quant.volume.tree.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='inventory_quantity']" position="attributes">
                <attribute name="force_save">1</attribute>
            </xpath>
            <!-- <xpath expr="//field[@name='product_id']" position="after">
                <field name="length"/>
                <field name="width"/>
                <field name="height"/>
            </xpath> -->
            <xpath expr="//field[@name='inventory_diff_quantity']" position="after">
                <field name="on_hand_units" sum="Total" optional="show"/>
                <field name="counted_units" force_save="1" optional="show"/>
                <field name="difference_units" optional="show"
                       attrs="{'invisible': [('inventory_quantity_set', '=', False)]}"
                       decoration-muted="inventory_diff_quantity == 0"
                       decoration-danger="inventory_diff_quantity &lt; 0"
                       decoration-success="inventory_diff_quantity &gt; 0"
                       decoration-bf="inventory_diff_quantity != 0"/>
            </xpath>
        </field>
    </record>

    <record model="ir.ui.view" id="view_stock_quant_tree_volume_inherit">
        <field name="name">stock.quant.tree.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_editable"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="after">
                <field name="length"/>
                <field name="width"/>
                <field name="height"/>
            </xpath>
            <xpath expr="//field[@name='product_uom_id']" position="after">
                <field name="on_hand_units" sum="Total" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
